# CLAUDE.md

本文档为 Claude Code (claude.ai/code) 在处理此仓库代码时提供指导。

## 项目概述

这是一个使用纯 HTML、CSS 和 JavaScript 构建的 HTML 工具和迷你游戏集合。该仓库包含三个主要类别：

1. **实用工具** - 按用途拆成 5 个分类目录：`开发/`、`文本与格式/`、`编码与安全/`、`图像与文件/`、`生活与计算/`
2. **图一乐** - 迷你游戏和交互式演示，内部再分 `小游戏/`、`特效演示/`、`图集与测试/`
3. **没啥用的** - 实验性、纪念性，以及从工具区退役下来的项目

## 项目结构

分类目录名与 `index.html` 里的 `<h3>` 标题**逐字一致**，找工具时页面分类就是磁盘路径。

```
html-tools/
├── index.html                    # 主页面，包含全部导航链接
├── 开发/                         # 开发与配置类工具
│   ├── nginx可视化配置/          # Nginx 配置可视化
│   ├── 开发速查表/               # 开发者参考手册（体积最大，内含 docs/ 多层相对引用）
│   ├── CSS与HTML生成器/  css网格布局/  Adb在线执行器/
│   └── 手机APP协议跳转测试/  react-amap/
├── 文本与格式/                   # 文本对比与格式转换
│   ├── 文本差异对比/             # 三版各占一个子目录：按字/ 按词/ 按行/（各带自己的库）
│   ├── 时间戳/  转特殊字符/  驼峰与下划线转换/
│   └── JSON转JS/  yaml-properties-converter/  java一行的多行注释一行化/
├── 编码与安全/                   # 编解码、加密、隐写、指纹
│   ├── Base64/  JWT/  MD5加密工具/  2fa/  二维码工具/
│   ├── 图片隐藏数据分析/         # 元数据 / LSB 隐写 / 频谱盲水印（3 个页面）
│   └── 浏览器指纹/
├── 图像与文件/                   # 图片、视频、PDF、图床、下载
│   ├── PDF多功能编辑器/  视频转webp/  实况图片换封面/  图片转盲文/
│   ├── 白板/  文件强制下载工具/
│   └── 聚合图床/  官方图床/
├── 生活与计算/                   # 计算器与生活类
│   ├── 城乡居民养老金计算器/      # index.html 含利息 / 忽略利息版.html
│   └── 七天年化收益率计算器/  工资不止差一半计算器/  3D人体标注/
├── 图一乐/                       # 游戏和趣味项目
│   ├── 小游戏/                   # 单版本的是单文件，有多版本的套一层目录
│   │   ├── 2048.html  俄罗斯方块.html  扫雷.html  切水果.html  ...
│   │   ├── 小游戏统一风格.md     # 新增小游戏时的风格约定
│   │   ├── 贪吃蛇/  飞机大战/  消消乐/   # 各含 index.html 与 new.html / mi.html（mi = 小米 MiMo 写的）
│   │   └── 大话骰/  拳皇二稿版/  落日狂飙/
│   ├── 特效演示/                 # 3D机器手 / 3D蜘蛛网 / 黑客帝国瀑布特效 等 8 项
│   └── 图集与测试/               # SBTI测试 / bing壁纸预览 / AI二维码观赏
├── 没啥用的/                     # 实验性 / 退役项目（首页故意埋在最下面）
│   ├── index1.html               # 新样式首页（不再更新）
│   ├── 报废的书签/               # 遗留书签
│   └── 商品比价器/  快捷App搜索/  高德排线数据渲染/  中转探针/   # 从工具区退役下来的
├── css/  js/                     # 仅首页使用的共享样式与脚本
├── GitHub-zhCN251004.js          # GitHub翻译文件 AI不要读 里面是超大的GitHub翻译文件
│                                 # 外部油猴脚本按固定 URL 引用，不要移动
└── ikun.json                     # 外部服务读取该文件拿到对外提供的页面
```

## 开发命令

这是一个静态 HTML/JS 项目，大多数工具不需要构建系统。但某些项目有特定的构建要求：

### 一般开发
- 所有工具都是独立的 HTML 文件，可以直接在浏览器中打开
- 大多数项目不需要编译或构建步骤
- 开发时直接编辑 HTML、CSS 或 JS 文件即可

## 技术栈

- **前端**: HTML5, CSS3, 原生 JavaScript
- **框架**: 部分项目使用 Bootstrap、Vue.js (css网格布局)、React (react-amap)
- **物理引擎**: Verlet.js 用于 3D 蜘蛛网物理效果
- **构建工具**: Browserify 和 UglifyJS (仅用于 3D 蜘蛛网)

## 关键文件和入口点

- `index.html` - 主导航页面，五个分类小节的 `<h3>` 标题与分类目录名一一对应
- 各个工具目录包含自己的 `index.html`
- 大多数游戏是 `图一乐/小游戏/` 中的单个 HTML 文件
- `ikun.json` - 外部服务读它取对外页面清单，**动了工具路径必须同步改这里**

## 添加新工具

1. 判断它属于哪个分类，在对应分类目录下新建工具目录（不要再往仓库根目录放）
2. 添加包含工具功能的 `index.html` 文件
3. 在主 `index.html` 的**同名 `<h3>` 小节**下加链接，路径写根绝对形式 `/分类/工具名/index.html`
4. 遵循现有的自包含 HTML/CSS/JS 文件模式,第三方包也包含在该文件夹里面，不要引用别的文件夹的第三方包
5. 引用 `favicon.ico` 等仓库根资源时用 `/favicon.ico` 而不是 `../favicon.ico`，这样以后挪目录不会断

## 同一工具的多个版本

同功能的多个版本收在**同一个工具目录**下，不要在分类目录里各占一个位置。目录内部怎么放，
看这个版本是**一个文件**还是**一堆文件**——判断标准是「想单独打包一个版本发给别人时，能不能一眼挑出属于它的文件」：

**每个版本都是单个自包含 HTML** → 直接平铺在同一个目录里，用文件名区分。
拷一个文件走就能用，不会分不清。主推的那版做 `index.html`：
`城乡居民养老金计算器/{index.html, 忽略利息版.html}`、
`图一乐/小游戏/贪吃蛇/{index.html, new.html}`、
`飞机大战/{index.html, mi.html}`、`消消乐/{index.html, mi.html}`。

**版本各自带 css / js / lib / 图片** → 每个版本一个子目录，各自放 `index.html` 加自己的依赖，
父目录只负责归类、不放文件。混在一起就分不清哪个库是哪版在用：
`文本差异对比/{按字/, 按词/, 按行/}`
（`按字/` 带 mergely 5.0.0，`按行/` 带 `lib/` 里的 jQuery+CodeMirror，`按词/` 只有一个 html）。
这种情况 `ikun.json` 和首页要指到具体子目录，父目录下没有 `index.html`。

**不是「版本」而是一个多页工具**（页面之间互相跳转、共用同一套 `styles.css` / `js`）→ 保持一个目录别拆，
拆了会断内部链接、还要复制共用资源：`图片隐藏数据分析/{index.html, lsb-write.html, lsb-stencil.html}`、
`3D人体标注/{index.html, 内部.html}`、`图一乐/小游戏/大话骰/{index.html, 3d.html, demo.html}`。

首页把同一工具的几个版本写在同一个 `<li>` 里用 `|` 分隔。
只有一个版本的（`2048.html`、`扫雷.html`、`时间戳/` 等）保持原样，等真出了第二版再按上面处理。

## 代码风格指南

- 大多数项目都包含在单个 HTML 文件中
- CSS 通常嵌入在 `<style>` 标签中或单独的 `.css` 文件中
- JavaScript 通常在单独的 `.js` 文件中或嵌入在 HTML 中
- 整个项目使用中文注释和变量名
- Bootstrap 通常用于响应式布局

## 测试

- 没有设置自动化测试框架
- 在浏览器中打开 HTML 文件进行手动测试是标准方法
- 所有工具应在现代浏览器（Chrome、Firefox、Safari、Edge）中正常工作

## 部署

这是一个可以部署到任何 Web 服务器的静态网站：
1. 将所有文件复制到 Web 服务器
2. 确保保持目录结构
3. 通过 `index.html` 作为入口点访问

